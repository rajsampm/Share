
//Contact - gauravak007@gmail.com
//github  - www.github.com/gaurav1620

#include<bits/stdc++.h>

using namespace std;

int main(){
      // vector<pair<int,int>> v;
      //
      // for(int i = 0 ; i < 10;i++)
      //   v.push_back({i,i*i});
      //
      // vector<int>::iterator itr;
      // for(int i= 0;i < v.size();i++){
      //   cout<<v[i].first<<" : "<<v[i].second<<endl;
      // }

      // set<int> s;
      // //set<int>::iterator itr;
      // s.insert(1);
      // s.insert(4);
      // s.insert(2);
      // s.insert(6);
      // for(auto itr = s.begin();itr != s.end();itr++){
      //   cout<<*itr<<endl;
      // }

      // map<string,int> m;
      // m.insert({"Raj" , 22});
      // m.insert({"Gaurav" , 23});
      // m.insert({"Nick" , 12});
      // m.insert({"Tom" , 14});
      //
      // for(auto itr = m.begin();itr != m.end();itr++){
      //   cout<<"Name : "<<itr->first<<" has age : "<<itr->second<<endl;
      // }

      // vector<int> v = {4,6,8,2,9,10};
      // auto itr = v.begin();
      //
      //
      // advance(itr,3);
      // auto newOne = prev(itr,3);
      // cout<<*newOne<<endl;

      // vector<int> arr1 = {1,2,3,4,5};
      // vector<int> arr2 = {10,20,30};
      //
      // auto itr = arr1.begin();
      // advance(itr,3);
      //
      // //inserter()
      // copy(arr2.begin(),arr2.end() , inserter(arr1,itr));
      //
      // for(unsigned int i = 0 ; i < arr1.size();i++){
      //   cout<<arr1[i]<<endl;
      // }

    return 0;
}
